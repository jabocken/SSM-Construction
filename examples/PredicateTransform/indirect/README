- objects.c
Object-oriented style of programming
Contains indirect calls, compiled with gcc (both O0 and O3)

- switch-input
Switches based on reading input
Contains indirect jump, compiled with gcc (O0)

-- callback.c
Contains a C-style callback, which is randomly picked from 1 of 3 functions.
Compiled with gcc (both O0 and O3 are interesting)

 -- function_pointer.c
Contains indirect function calls to external functions
Compile with cc and the -lm option

- goto.c
Contains a jump table and uses goto to jump to random places
Compiled with gcc (both O0 and O3 are interesting)
